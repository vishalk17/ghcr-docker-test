# Use official Nginx base image
FROM nginx:alpine

# Optional: Copy your custom HTML file (if needed)
# COPY index.html /usr/share/nginx/html/index.html

# Expose default HTTP port
EXPOSE 80

# Start Nginx (already defined in base image, but you can override)
CMD ["nginx", "-g", "daemon off;"]

